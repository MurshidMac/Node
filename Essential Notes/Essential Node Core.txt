In javascript
==============
As we know the window is our global object.
In node JS the global object is global (Namespace)
===============
So to run a node file we need to "node specificJSfile.js"	// Node call from the cmd
